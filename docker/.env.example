# Optional overrides for docker/docker-compose.yml
# Copy to docker/.env if you need non-default Postgres settings.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=industrial_platform
POSTGRES_PORT=5432
